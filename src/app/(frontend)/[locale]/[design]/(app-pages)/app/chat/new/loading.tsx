import { CreateChatSkeleton } from '@/features/chat/components/chat-overview-view/create-chat-skeleton';
import type React from 'react';

const Loading: React.FC = () => {
  return <CreateChatSkeleton />;
};

export default Loading;
